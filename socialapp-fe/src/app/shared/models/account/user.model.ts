export interface User {
    id: string;
    email: string;
    knowAs: string;
    firstName: string;
    jwt: string;
    gender: string;
    photoUrl: string;
    refresh_token: string;
    roles: string[];
}
